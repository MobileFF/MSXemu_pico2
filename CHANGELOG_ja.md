# 変更履歴

本プロジェクトの主な変更点をこのファイルに記録する。英語版は[CHANGELOG.md](CHANGELOG.md)を参照。

## [Unreleased]

### 追加

- **Z80 CALL/RSTフックによる拡張機構**: `/sd/msx/ext/`またはオンボードフラッシュの`/ext/`にモジュールを置くだけで起動時に自動読み込みされる(`mp/msx_ext.py`)。`main.py`の編集は不要。姉妹エミュレータPB-1000と同じ設計。
- フック機構を補うデバッグ用API: `msx.debug_set_cpu()`(`msx.debug_cpu()`の書き戻し版。`msx.debug_cpu()`自体も`bc`/`de`/`hl`/`ix`/`iy`を返すようになった)、`msx.debug_poke()`、`msx.debug_get_slot()`/`msx.debug_set_slot()`。詳細は`doc/extension_api.md`/`doc/extension_api_en.md`参照。
- 実機検証済みの制約事項を記載: 割り込みベクタ(例: `0x0038`)をフックすると、フック機構が置き換え対象と一緒に本来のハンドラの`EI`命令もスキップしてしまうため、割り込みが恒久的に無効化される。

### 修正

- **ランタイムメニュー(GUI+F7)の実機クラッシュ**(`OSError: [Errno 5] EIO`): メインループはLCDのDMA転送を開始した直後、転送が完了する前にキーボードをポーリングする(高速化のための意図的なパイプライン化)。そのタイミングでメニューのホットキーが押されると、メニュー自身の最初の描画が同じDMAチャンネル・SPIペリフェラルを、前の転送の完了を待たずに再設定・再起動してしまっていた — これはHDMIブリッジとは無関係の競合状態で、当初の調査はHDMI側に焦点を当てていた。共有SPI1バスにメニューが触れる前に、進行中の転送を`msx.wait_display()`で確実に完了させることで修正。
- **上記クラッシュ修正後に顕在化したカートリッジ差し替え失敗**(`cart_alloc() failed`): `MICROPY_C_HEAP_SIZE`の非圧縮アロケータが、小さいカートのブロックを解放した後でも32KBの確保に失敗することがあった(ヒープ全体では名目上の空きがあっても)。`msx_cart_alloc()`は、カート差し替えのたびに`free()`/`malloc()`するのではなく、容量を記録した1つのバッファを同一スロット内で使い回すよう変更(追加のRAM予算は不要 — 先にCヒープの拡大や静的バッファも試したが、いずれも同じ分だけ別のMicroPython GCヒープを圧迫し、起動時のモジュールimportが壊れる結果になった)。
- **`config.txt`読み込み失敗が黙ってデフォルト値にフォールバックしていた問題**: `config.txt`を開く際の一時的なSD読み取りエラーが「設定ファイルが存在しない」場合と区別できず、`bios=`を正しく設定していても紛らわしい「BIOS not found」エラーになっていた。`load_config()`は数回リトライし、それでも読めない場合は警告を出すようにした。
- **SD読み書き速度が誤って400kHzに固定されていた問題**: SDカードドライバの転送ごとのボーレートが、本来は初期の低速ハンドシェイク専用の定数を使い回してしまっており、LCD/SPIの設定速度に関わらず全てのブロック読み書きが400kHzで実行されていた。専用の`SD_DATA_BAUD`(4MHz)に分離 — 信頼性とスループットの両方の修正。
- **メニューのクラッシュでゲームセッションが終了してしまう問題**: ランタイムメニューが予期しない例外を捕捉し、完全なトレースバックを内蔵フラッシュの`/crashlog.txt`に記録し(SDには書かない — 直前に失敗したカードへ書くのは堂々巡りになるため)、LCDが固まっていた場合は再初期化を試み、`main.py`ループ全体をクラッシュさせるのではなくゲームプレイを再開するようにした。
- メニューのステータス/エラーメッセージ(完全な`OSError`メッセージ等)がシリアルコンソールにも出力されるようになった — LCDの31文字制限で今まで黙って切り詰められていた。

### 追加(2026-09-04/05)

- **HDMI Display Settingsメニュー(GUI+F7)**: LCDパネル機種・回転・HDMIボーレート、および新設の`boot_exclusive`設定を、既存のAudio/HDMI Settings画面と並んでランタイムメニューから編集できるようにした。4項目とも再起動が必要(`msx.init_display_hardware()`/`msx.init_hdmi_output()`が起動時に一度だけ読むため) — 画面上にもその旨表示され、ENTERで即座に反映されるのではなく`msx.ini`へ書き込まれる。
- **`boot_exclusive`設定**: `display=lcd`または`display=hdmi`(`both`は対象外)と組み合わせると、起動時に使わない側のハードウェア初期化を丸ごとスキップする — `display=hdmi`ならLCDパネルのリセット・初期化シーケンス自体を省略(実際の起動時間短縮になり、LCDを物理的に接続していなくても動作する)、`display=lcd`なら`hdmi=1`でも`msx.init_hdmi_output()`を省略する。どちらも後からメニューでその場でONにすることは可能。
- **ROMセレクタのフォルダナビゲーション**: `select_rom()`がSDカードのルートから探索するようになり(以前は`/sd/msx`固定)、サブフォルダにも入れるようになった — UP/DOWNで移動、ENTERでフォルダを開く/`.ROM`を選択、ESCで1階層戻る(ルートではキャンセル)。キーボード未接続時は最初に見つかったROMを再帰的に自動選択するようフォールバックする。
- **HDMI送信のDMA化**(`msx_render_to_hdmi()`): 1フレーム分をまとめて静的バッファへ構築し、192回の個別ブロッキング`spi_write_blocking()`呼び出しの代わりに1回のDMA転送(投げっぱなし、LCD側と同じ方式)で送るようにした。`hdmi=1`時の実機FPSは、従来の完全ブロッキング送信(10MHzで約40ms/フレーム)から大幅に改善した。
- **hdmi_bridge_receiver**: 左上の受信確認用マーカーを、実際のゲームプレイ中フレームでは非表示(黒で上書き)にし、メニュー/UI画面(ROMセレクタ、ランタイムメニュー)でのみ表示するようにした — 送信側が元々送っていたフレームごとのヘッダーの`bpp`値(4=ゲーム、8=メニュー)を流用しており、プロトコル変更は不要。

### 変更(2026-09-04/05)

- **設定ファイルの名称・配置変更**: `/sd/msx/config.txt` → `/sd/msx.ini`(SDカードのルート)。既存のカードは設定ファイルを移動・改名する必要がある。
- **HDMIボーレートのデフォルトを10MHz→8MHzに変更**: 実機検証の結果、10MHzではこの配線において受信パレットが確実に化けることが判明(例: 白が緑になる) — 伝送方式のバグではなく、純粋に電気的・タイミング的なマージンの限界。5MHzと8MHzはいずれも問題なし(8MHzの方がわずかに高速)。詳細は`doc/hdmi_bridge_phase2_report.md`参照。

### 修正(2026-09-05)

- **上記機能追加後に発生した起動時の`MemoryError`**(`msx_menu.py`が新規ROMブラウザ+Display Settingsのコードで約21%肥大化し、実機での起動時コンパイルに失敗するようになっていた): Display Settings画面とROMフォルダブラウザをそれぞれ独立したモジュール(`msx_display_settings.py`、`msx_rom_browser.py`)に分離し、起動時の即時importチェーンではなく、実際に初めて使われた時点で遅延コンパイルされるようにした。`msx_menu.py`自体は今回のセッションでの変更前よりも小さくなっている。
- **SDルート配下のフォルダにUTF-8として不正なファイル名がある場合のROMブラウザのクラッシュ(`UnicodeError`)**(別OS/ファイルシステム由来のファイル名等が原因になりうる): `uos.ilistdir()`がデコード中に`UnicodeError`を送出することがあり、既存の`OSError`処理では捕捉できずブラウズ全体がクラッシュしていた。同様に捕捉するようにし、該当ディレクトリは読み取り不可としてスキップするだけになった。

### 追加(2026-09-05/06)

- **LCDバックライト制御**(`msx.set_backlight()`): `display=hdmi`のとき自動的に消灯するようにした(このモードでもLCDパネル自体は初期化されるが描画は一切行われないため、以前は固まった/古い画面が点灯したままだった) — HDMI Settingsメニューから`display`をその場で変更した際にも即座に反映される。
- **HDMI受信側ハードウェアリセットライン**: 送信側の空きGPIO(`HDMI_RESET_PIN` = GP13)を受信側Pico 2のRUNピンに直結し、`init_hdmi_output()`実行前、およびメニューからHDMIをその場でONにした際に、短くLowパルスを送るようにした。HDMIケーブルを接続したまま受信側の電源を入れる(またはホットプラグする)と、受信側が不正な状態のまま起動してしまう実機で確認済みの問題を解決する(TMDSライン経由のバックフィードがパワーオンリセットのタイミングを乱していると推測)。アイドル時はHi-Z — `hdmi_bridge_receiver`側の`notes/sender_reset_line.md`の設計通りで、受信側ファームウェアの変更は不要。
- **60fps フレームレート調整**: 上記HDMIのDMA最適化により、メガロム以外のカートで実機FPSが恒常的に70fpsを超えるようになり、`run_frame()`呼び出しごとに一定量進むMSXクロック時間が実時間より先行してしまう(ゲームが早送りになり、PSGの音程も上がる)という問題が発生した。メインループの各反復で、1/60秒の残り時間をsleepで消化するようにした。既に60fps以下の反復(低速なカート、`display=both`など)には影響しない。
- **カートリッジごとのローテーションセーブ**: ランタイムメニューのSave/Load StateとF5/F8ホットキーの両方が、単一の共有ファイルではなく現在読み込まれているカートの実パス(`cart_path`)に基づくようになった — 各カートのセーブはROM本体の隣に置かれ(例: `/sd/games/Foo.ROM` → `Foo.0.sav`、`Foo.1.sav`、…)、カートごとに最大`msx_menu.MAX_SAVE_SLOTS`(10)件の過去の状態を保存し、セーブのたびにローテーションする。ランタイムメニューの「Load State」は常に最新の1件を読み込むのではなく、スロット選択画面(`msx_save_slots.py`、遅延import)を開くようになった。F5/F8ホットキーはクイックセーブ/クイックロードの利便性のため、引き続き常にスロット0(最新)のみを対象とする。
- **Swap Cartridgeの初期表示フォルダ**: 常にSDルートから開始するのではなく、現在読み込まれているカートが置かれているフォルダ(`start_dir`)から開始するようにした — `".."`で従来通りルートまで遡ることもできる。

### 修正(2026-09-06)

- **Swap CartridgeにおけるESCの回帰バグ**(上記`start_dir`機能の副作用): 開始フォルダがSDルートより下の場合、ESCを1回押すと1階層上に戻るだけでキャンセルされず、キャンセルにはルートまで階層の数だけESCを押す必要があった。ESCは階層の深さに関わらず常に即座にキャンセルするよう修正 — 上の階層に移動するには従来通り`".."`を選んでENTERする。
- **`OSError(5,)`(EIO)発生後、ランタイムメニューが二度と開けなくなる問題**: `sdcard.py`の`readblocks()`/`writeblocks()`が、SDコマンド応答の失敗(この共有SPIバスで既知の実機タイミングマージンの問題 — `msx_menu.py`の`lcd_suspend()`のコメント参照)を検知した際、リトライなしでいきなり`OSError(5)`を送出しており、それをMicroPythonのVFS層がカード全体の致命的エラーとして扱ってしまい、以後セッション終了までSDアクセスが全て同様に失敗していた(単発では次の試行なら成功していたはずのものも含めて)。両メソッドとも、諦める前に最大3回(5ms間隔、毎回`spi.init()`をやり直す)リトライするようにした — 単発的なバスの取りこぼしを、セッション終了級の失敗ではなく一過性のヒッチアップに戻す。

### 変更・削除(2026-09-06)

- **LCDとHDMI出力を完全に排他化**: 同時出力(`display=both`)を、単に非推奨にするのではなく廃止した。この2つのSPIモードを毎フレーム切り替えることは、実機で最終的にHDMI信号の破損/喪失とLCD側のグリッチを引き起こすことが既に判明していたため(`msx_core.c`の`hdmi_apply_spi_settings()`と`doc/hdmi_bridge_phase2_report.md`参照)。`display`は`lcd`(デフォルト)または`hdmi`のみ受け付ける。
- **独立した`hdmi=0/1`設定キーと、HDMI SettingsメニューのHDMI: On/Off項目を削除**: 出力が排他になったことで、`display=hdmi`単独でHDMIブリッジの選択と有効化を兼ねるようになり、独立したOn/Off状態そのものに意味がなくなったため。HDMIハードウェアは`display=hdmi`のとき(起動時、またはHDMI SettingsメニューでDisplayフィールドをその場で`HDMI`に切り替えた瞬間)にのみ初期化される。LCDパネルも対称的に、`LCD`に戻した瞬間に遅延初期化されるため、起動時にはどちらか一方しか初期化されなくても、どちら向きの切り替えも再起動なしで機能する。
- **`boot_exclusive`設定キーと、Display SettingsメニューのExclusive Boot項目を削除**: その挙動(使わない側のハードウェア初期化を起動時にスキップする)は、`display=lcd`/`display=hdmi`が常にそう動作するというだけの話になった — もはや例外扱いすべき`both`モードが存在しないため。
- 既存の`msx.ini`で`hdmi=`/`boot_exclusive=`/`display=both`を使っている場合、前者2つは削除し、`display=both`は`lcd`または`hdmi`に変更してください。`load_config()`は未知/削除されたキーを単に無視するだけなので致命的にはなりませんが、`display=both`は`lcd`にフォールバックされます。

### 追加・変更(2026-09-06 続き)

- **「HDMI Settings」と「Display Settings」のランタイムメニューを1つの「Display Settings」画面に統合**: 出力が完全排他になったことで(上記参照)、旧HDMI Settings画面は`Display`/`Frame Skip`だけの薄い画面になり、LCDパネル/回転/HDMIボーレートの設定と別メニューに分ける意味が薄れていたため — 両方とも同じ画面(`msx_display_settings.py`)から編集する。
- **「Reinit <LCD/HDMI> now」アクションを新設**: 再起動せずにその場で現在使用中の側のハードウェア初期化を再実行する。長時間HDMIでプレイし続けると画面が真っ黒になったまま復帰しなくなる実機報告を受けて追加 — ランタイムメニュー自体(パレットに依存しない別の送信経路で描画される)は正常に表示され続けていたことから、HDMI受信側が保持しているカラーパレットテーブル(起動時に一度だけ送信)が長時間のセッションで何らかの理由でズレてしまう症状と一致する。同じ初期化処理を再実行することは、単に伝送経路をリセットするだけでなく実際にパレットを再送するため、実効性のある修正になっている。Display フィールドをその場で切り替えた際に呼ばれるのと同じ遅延初期化コールバックを再利用しているため、どちらの経路でも実績のあるコードパスが動く。

### 修正(2026-09-06、重大 — display=hdmiで起動時にハングする問題)

- **`display=hdmi`で起動すると完全にハングする問題**(シリアル出力が「HDMI bridge output enabled…」の直後で止まり、ROM選択画面もどちらの出力にも表示されず、以後ログも一切出ない — 復旧にはハードウェアリセットが必要): 同日先に行ったLCD/HDMI完全排他化の回帰バグ。HDMI送信系の全関数(`msx_send_hdmi_palette()`/`msx_render_to_hdmi()`/`msx_clear_hdmi()`/`msx_render_to_hdmi_raw332()`)がヌルチェックなしで`(spi_inst_t *)msx->spi_inst`として参照している共有SPIインスタンスは、これまで`msx_init_display_hardware()`(LCD側)でのみ設定されていた。`display=hdmi`での起動は今回の変更で常にこの関数を丸ごとスキップするようになったため、`msx_init_hdmi_output()`の最初の送信(`msx_send_hdmi_palette()`)がNULLのSPIインスタンスを参照してしまい、フォルトハンドラ未設置の環境で`spi_write_blocking(NULL, …)`が呼ばれ、クラッシュではなく基板全体のハングという形で現れていた。この不備自体は今回の変更以前から潜在していた(明示的な`boot_exclusive=1`+`display=hdmi`の組み合わせでも同様に発生し得た)が、`display=hdmi`でLCD初期化を省略することが例外ではなく常態になって初めて必ず踏むようになった。`msx_init_hdmi_output()`自身が`msx->spi_inst = spi1`を設定するように修正(本プロジェクトはどちらの側が起動時のdisplayを担うかに関わらず、HDMIブリッジは常にSPI1に配線されているため)、LCDが初期化されたかどうかに依存しないようにした。**ファームウェアの再ビルドが必要**です(`src/msx/msx_core.c`のC側修正のため)。
- **`display=hdmi`セッション中にランタイムメニュー(GUI+F7)を開くと受信側がNo Signalになる問題**: `msx_render_to_hdmi_raw332()`(MSXの16色ハードウェアパレット外の任意色を使うメニュー/UI用の送信経路 — パレット参照方式は使えない)は、`msx_render_to_hdmi()`が2026-09-04に脱却した「1行ずつ`spi_write_blocking()`でブロッキング送信」という元々の方式のまま残っていた — メニューを再描画するたびに約49KBを丸ごとブロッキング送信していたことになる。この失敗モードには本プロジェクトに既に実機での前例があった(`msx_clear_hdmi()`の既存コメント参照: 完全な`PKT_FRAME`のブロッキング送信は「受信側が断続的にHDMI同期を失う一因にもなっていた」とあり、それが約9バイトの専用コマンドを使っている理由そのもの)。`msx_render_to_hdmi()`と同じ「フレーム全体を先に構築してから1回のDMA転送」方式に変換した。新設した全フレームバッファ(1ピクセル1バイト、49152バイト)は、既存のパレット参照用バッファと`union`でメモリを共有している(両者が同時に使われることはないため) — 別々に確保する形で最初に試したところ「GcHeap is too small」でリンクに失敗したため、この2つの合計サイズをこれ以上気軽には増やせない状況だった。**ファームウェアの再ビルドが必要**です。
- **起動時に再び`MemoryError`が発生する問題**(`main.py`の`from msx_menu import (...)`、以前のセッションでの`MemoryError`と全く同じ発生箇所 — さらに最初の修正試行時点ではアロケーションサイズ1336バイトまで完全に一致): 本日の一連の変更(表示排他化、メニュー統合、それぞれの説明コメント追加)で`main.py`自体がかなり肥大化しており、直後に`msx_menu.py`をコンパイルする時点でまだ常駐している`main.py`のコンパイル済みフットプリントと合わせて、合計のピークが再び限界を超えてしまっていた — `msx_menu.py`自体のサイズは(HDMI/Display Settings統合により)むしろ今日は縮小していたにも関わらず。
  - 最初の試行: `main.py`のモジュールレベルdocstringと残りの関数docstringをすべて通常の`#`コメントに変換(docstringは実行時に保持される文字列定数としてコンパイルされるが、`#`コメントは実行時コストがゼロになる)。**これだけでは不十分と判明** — 実機で再テストした結果、全く同じエラーが1バイトも違わず再現した。本ボードの非圧縮GCヒープでは、あちこちの小さな確保領域を解放しても、大きなコンパイルが実際に必要とする単一の連続ブロックが必ずしも大きくなるとは限らない — この`MemoryError`の元々のバージョン(上記2026-09-05の項目参照)でも単なるトリミングだけでは不十分で、より踏み込んだ対策が必要だったことと一致する。
  - 実際に効いた修正: ランタイムメニュー本体(`show_emulator_menu()`、`_show_audio_settings_menu()`、およびそれぞれの描画ヘルパー — `msx_menu.py`に残っていた即時コンパイル対象の中で単独最大のかたまり)を新設の`msx_runtime_menu.py`に分離し、GUI+F7が実際に押された最初の1回だけ遅延importするようにした(`msx_display_settings.py`/`msx_rom_browser.py`/`msx_save_slots.py`と全く同じパターン)。`msx_menu.py`自体の即時コンパイル対象は約42KBから約29KBに縮小した。`main.py`のdocstring→コメント変換もそのまま残している(無害であり、規模は小さいものの実際の削減にはなっているため)。純粋なMicroPython側の変更のため、ファームウェアの再ビルドは不要です。
- **起動時のROM選択メニュー表示直後に映像が乱れ、No Signalに陥る問題(`display=hdmi`)**: `msx_render_to_hdmi_raw332()`の2026-09-06のDMA化(上記「GUI+F7でNo Signal」の修正参照)により、メニュー/UI画面のフレームもゲーム画面の`msx_render_to_hdmi()`と同様にDMA転送を投げっぱなしにするようになった — 8MHzでは約49KBのフレーム全体の転送完了に約49msかかる。これらの画面を描画した直後にSDへの重い処理を行う呼び出し元(起動時ROM選択メニュー→`load_cart_smart()`、フォルダ移動→次のディレクトリ一覧取得、`msx_runtime_menu.py`の「Loading…」/「Saving…」描画→カート/ステートI/O、`msx_display_settings.py`/Audio Settings→`save_config()`)は、すべて`msx.wait_display()`または`hdmi_suspend()`固有の固定20ms待機だけで転送が終わっていることを期待していたが、約49ms(ゲーム画面サイズなら約25ms)の転送に対しては全く足りていなかった。さらに悪いことに、`msx_wait_display()`自体が`display_ready`(LCD専用フラグ、既にコメント内で既知のギャップとして記載済み)でしかガードされておらず、`display=hdmi`でLCDが一度も初期化されていない場合は完全に何もしないno-opになっていた — その結果、直前のフレームのDMA転送がまだ実際にビットを送出している最中に、SDドライバの`spi.init()`呼び出しが同じ物理SPI1ペリフェラルのボーレート/モードレジスタを書き換えてしまっていた。最初の修正: `msx_wait_display()`のガードに`hdmi_ready`のチェックを追加(これ自体は今も正しい修正 — `msx_init_hdmi_output()`が無条件で`msx->spi_inst`を設定するようになった今は安全)、加えて上記の各箇所すべてに`wait_display()`/`msx.wait_display()`呼び出しを追加した。
- **上記の修正をさらに単純化**: メニュー/UI画面はゲームプレイと違ってFPS目標を守る必要がそもそもなく、`msx_render_to_hdmi_raw332()`が転送を投げっぱなしにする理由自体がなかった。ペイロード全体を1回の`spi_write_blocking()`呼び出しで送り、実際に完了しCSがデアサートされてから返る、完全に同期的な方式に戻した。これにより、上で追加した`wait_display()`呼び出しは構造的にすべて不要になったため、再び全て削除した — 例外は`main.py`の`save_state()`/`load_state()`(F5/F8ホットキー)内の2箇所のみ残している。こちらは別の、今も現実に起こりうるケース(ゲーム自身の`msx_render_to_hdmi()`はFPSのため今も意図的に投げっぱなしのままで、F5/F8はゲームプレイ中いつでも発火しうる)を守るためのもの。結果として、将来メニュー画面を追加するたびに呼び出し箇所を覚えておく必要がなくなり、`msx_core.c`内の自己完結した1箇所の修正だけで済むようになった。**ファームウェアの再ビルドが必要**です。
- **ランタイムメニューから「Display Settings」を開くとゲームプレイ中に`MemoryError`が発生する問題**: 上記の起動時の`MemoryError`群と違い、この遅延import(`msx_display_settings.py`)は起動直後のヒープではなく、カート/エミュレーション状態(メガロムのフラッシュキャッシュ、フレームバッファ、音声バッファなど)で既に断片化したヒープと競合する — 起動直後なら問題なくコンパイルできる同じモジュールでも、実際にしばらくゲームをプレイした後だと失敗しうる。4つの遅延import(`msx_rom_browser`、`msx_runtime_menu`、`msx_save_slots`、`msx_display_settings`)それぞれの直前に明示的な`gc.collect()`を追加してデフラグするようにした(`msx_runtime_menu.py`のカート読み込みコードで既に使われていたのと同じ防御的パターン)。あわせて`msx_display_settings.py`と`msx_runtime_menu.py`に残っていたdocstringも`#`コメントに変換し、余裕を増やした。純粋なMicroPython側の変更のため、ファームウェアの再ビルドは不要です。

### 追加(2026-09-06、さらに続き)

- **GUI+P / GUI+ESC グローバルホットキー**: `display=hdmi`でよく使う2つの復旧操作を、ランタイムメニューを一切開かずに実行できるようにした(`msx_runtime_menu.py`/`msx_display_settings.py`のimport自体が不要になるため、ヒープに優しく、画面を直すだけが目的なら操作も速い)。GUI+Pは`display`を`lcd`/`hdmi`間でその場で切り替え(Display Settingsの「Display」フィールドと同じ効果)。GUI+ESCは現在使用中の側をその場で再初期化(「Reinit ... now」行と同じ効果) — 長時間セッションでのHDMI「No Signal」問題への直接の復旧手段。どちらも共有SPI1ペリフェラルを再設定する前に、投げっぱなしのDMA転送があればまず`msx.wait_display()`でドレインする(GUI+F7の既存のガードと同じ考え方)。

### 修正(2026-09-08)

- **通常(インRAM)カートリッジを読み込んだ後にメガロムを読み込むと`MemoryError`になる問題**: `get_rom_load_buf()`が使う共有スクラッチバッファは、セッション中「初めてメガロムを読む瞬間」に遅延確保される設計だった — この手順の場合、それは起動直後よりずっと後(既にゲームをプレイし、Swap Cartridge経由で`msx_runtime_menu.py`/`msx_rom_browser.py`が既にimportされた後)になり、起動直後よりヒープがかなり断片化した状態になっていた。この確保の直前に`gc.collect()`を追加する対応(最初の試行)は**実機で不十分と判明**した — 同じ`MemoryError`が再現した。本ボードの非圧縮GCヒープでは、`gc.collect()`は実際には存在しない大きな連続領域を作り出すことはできない。(a)バッファを8KB→4KBに縮小(メガロムのマッパー選択コードは確実に先頭数KB以内に収まる、この用途には十分な範囲)、(b)`load_cart_smart()`がこの確保自体の失敗を許容し、ヒューリスティックがパターンを見つけられなかった場合と同様にKONAMIマッパー(推測)にフォールバックしてカート読み込み全体を中断しないようにする、の2点で本当に修正した。このスクラッチバッファの確保に失敗しただけではメガロムのロードが失敗しなくなった。

### 削除(2026-09-08)

- **単体のF5(クイックセーブ)/F8(クイックロード)ホットキー**: これらを押すと、セーブ/ロード処理の後に通常のF5/F8キー入力もそのままMSX本体に転送されてしまっていた(`apply_hid_report()`には、そのキーが既にセーブ/ロード用に「消費」済みかどうかを知る手段がないため)。これはF5/F8を自身の機能に使うMSXソフトと衝突しうる。セーブ/ロードは引き続き実行時メニュー(GUI+F7)の`Save State`/`Load State`から利用可能 — GUI+F7自体はMSXキーマトリクスへ転送されることが一切ないため、この問題は起こらない。`msx_keymap.py`の特別扱い用エクスポートから`HID_F5`/`HID_F8`を削除(F5/F8は`HID_TO_MSX`経由の通常のMSXキーとしては引き続き動作し、影響なし)。`main.py`の`save_state()`/`load_state()`関数は、他に呼び出し元がなくなったため呼び出し箇所ごと削除。`doc/usage_guide.md`/`_en.md`、`doc/ext_hooks_guide.md`/`_en.md`のドキュメントも合わせて更新(後者2つは元々抜けていたGUI+P/GUI+ESCの行も追加)。

### ドキュメント(2026-09-08)

- **今週一連の設定・メニュー変更に、ユーザー向けドキュメントを追従させた**: `README.md`/`_en.md`、`doc/usage_guide.md`/`_en.md`、`doc/ext_hooks_guide.md`/`_en.md`、`doc/dev_guide.md`/`_en.md`が、数日前に`msx.ini`へ改名・SDルートへ移動したはずの`config.txt`のままだったり、カートリッジごとのローテーション式セーブに置き換わったはずの単一`save.bin`のままだったり、そもそも「Display Settings」メニュー項目とその`msx.ini`キー一式(`display`/`hdmi_frame_skip`/`hdmi_baud`)がまるごと記載漏れになっていたりした。特に`ext_hooks_guide.md`/`_en.md`の「完全リファレンス」節(設定キーの唯一の正式な一覧)は大幅に書き直し、HDMI関連3キーの追加、再起動要否の注記、Display Settingsサブ画面の解説(既存のAudio Settings解説と同じ形式)、廃止済みの`hdmi`/`boot_exclusive`キーと`display=both`値についての明記を行った。
